import React from 'react';

import SignupContainer from '../containers/SignupContainer';

const Home = () => {
    return (
        <div>
            <SignupContainer />
        </div>
    );
}

export default Home;
